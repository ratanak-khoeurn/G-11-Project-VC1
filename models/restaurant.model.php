
// function getStoreRestaurant(string $res_name, ){
    // global $connection;
    // $stetoment = $connection->prepare('insert into restaurants (res_name) values(:resname)');
    // $stetoment->execute([
        // ':resname'=> $res_name,
    // ]);
    // return $stetoment->rowCount()>0;



// }